# Optional. Neon (or any Postgres) URL so news edits persist on Vercel.
# Without it, the public site still serves the LinkedIn catalogue.
DATABASE_URL=

# Optional. Better Auth secret for the news-desk sign-in.
BETTER_AUTH_SECRET=
